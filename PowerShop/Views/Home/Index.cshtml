@model IEnumerable<Product>
@{
    ViewData["Title"] = "فروشگاه قدرت و سرعت ";
}

<div class="text-center">
    <h1 class="display-4">به فروشگاه من خوش آمدید</h1>
    <div class="row">
        @foreach (var item in Model)
        {

           <partial name="_BoxProduct" model="@item"/>
        }
    </div>
</div>
